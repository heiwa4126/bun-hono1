{
	"name": "bun-hono1",
	"scripts": {
		"start": "bun run src/index.ts",
		"dev": "bun run --hot src/index.ts",
		"format": "oxfmt",
		"lint": "oxlint",
		"check": "oxfmt && oxlint --fix"
	},
	"dependencies": {
		"hono": "^4.12.25",
		"node-unix-socket": "^0.2.7"
	},
	"devDependencies": {
		"@types/bun": "^1.3.14"
	}
}
