{
	"name": "bun-hono1",
	"scripts": {
		"dev": "bun run --hot src/index.ts",
		"format": "oxfmt",
		"lint": "oxlint",
		"check": "oxfmt && oxlint --fix"
	},
	"dependencies": {
		"hono": "^4.12.25"
	},
	"devDependencies": {
		"@types/bun": "latest"
	}
}
